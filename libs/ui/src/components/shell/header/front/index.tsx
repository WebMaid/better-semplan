import { UIHeaderFrontComponent } from "./component";

export interface IndexProps {}

export const UIHeaderFront = (indexProps: IndexProps) => {
	return <UIHeaderFrontComponent {...indexProps} />;
};
