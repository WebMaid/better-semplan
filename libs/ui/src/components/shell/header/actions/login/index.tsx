import { UILoginButtonComponent } from "./component";

export interface IndexProps {}

export const UILoginButton = (indexProps: IndexProps) => {
	return <UILoginButtonComponent {...indexProps} />;
};
