import { UIHeaderActionsComponent } from "./component";

export interface IndexProps {}

export const UIHeaderActions = (indexProps: IndexProps) => {
	return <UIHeaderActionsComponent {...indexProps} />;
};
