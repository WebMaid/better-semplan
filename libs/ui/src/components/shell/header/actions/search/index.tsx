import { UISearchButtonComponent } from "./component";

export interface IndexProps {}

export const UISearchButton = (indexProps: IndexProps) => {
	return <UISearchButtonComponent {...indexProps} />;
};
